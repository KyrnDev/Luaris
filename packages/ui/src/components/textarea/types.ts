export interface ILxTextareaProps {
	placeholder?: string,
	disabled?: boolean,
	readonly?: boolean,
	minRows?: number,
	maxRows?: number,
	maxLength?: number,
	resizable?: boolean,
	showCounter?: boolean,
}
