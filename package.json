{
	"name": "monorepo",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"hooks:setup": "git config core.hooksPath .githooks",
		"lint": "eslint",
		"test": "bun run --filter \"@luaris/ui\" test:run",
		"test:coverage": "bun run --filter \"@luaris/ui\" test:coverage",
		"ui:dev": "bun run --filter \"@luaris/ui\" dev"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@antfu/eslint-config": "^7.4.3",
		"@types/bun": "latest",
		"eslint": "^10.0.0"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
